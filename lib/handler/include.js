var Handler = require("../handler"),
    assert   = require("assert"),
    _        = require("lodash"),
    ImportHandler;

/**
 * @class ImportHandler
 * @extends Handler
 */
ImportHandler = Handler.extend(
    /**
     * @lends ImportHandler.prototype
     */
    {
        //
    }
);

module.exports = ImportHandler;